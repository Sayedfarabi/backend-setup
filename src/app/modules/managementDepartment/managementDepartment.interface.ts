/* eslint-disable no-unused-vars */
// import { Model } from 'mongoose'

export type TManagementDepartment = {
  name: string
  description: string
}

// export interface ManagementDepartmentModel
//   extends Model<TManagementDepartment> {
//   isUserExists(id: string): Promise<TManagementDepartment> | null
// }
